apiVersion: v1
kind: Service
metadata:
  name: worldcup-app
  labels:
    app: worldcup-app
spec:
  type: ClusterIP
  selector:
    app: worldcup-app
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
