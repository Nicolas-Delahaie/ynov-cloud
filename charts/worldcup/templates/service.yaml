apiVersion: v1
kind: Service
metadata:
  name: worldcup-app
  labels:
    app: worldcup-app
spec:
  type: ClusterIP
  selector:
    app: worldcup-app
  ports:
    - name: http
      port: 3000
      targetPort: 3000
