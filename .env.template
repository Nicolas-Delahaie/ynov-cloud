# PostgreSQL — identifiants partagés par tous les services (dev local)
# Source unique de vérité : lu par compose (interpolation) ET injecté dans app/test.
DB_HOST=db
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=worldcup2026

# GitHub Classic Personal Access Token avec le scope write:packages
# Créer sur https://github.com/settings/tokens > Generate new token (classic)
GITHUB_TOKEN=