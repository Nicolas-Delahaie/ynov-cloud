# Modèle de configuration — copier en `.env` puis adapter :
#   cp .env.example .env
# Le vrai `.env` est git-ignoré (pilier sécurité : pas de credentials dans Git).
DB_HOST=db
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=worldcup2026
